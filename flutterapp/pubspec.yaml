name: flutterapp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0

  web_socket_channel: ^3.0.1

  camera: ^0.11.0+1
  permission_handler: ^11.0.1
  shared_preferences: ^2.2.2

  flutter_gemma: ^0.12.0
  speech_to_text: ^7.3.0
  flutter_tts: ^4.2.5
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/llm/gemma3-1B-it-int4.task

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
